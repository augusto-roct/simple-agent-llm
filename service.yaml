apiVersion: v1
kind: Service
metadata:
  name: simple-agent-llm-service
spec:
  selector:
    app: simple-agent-llm
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
